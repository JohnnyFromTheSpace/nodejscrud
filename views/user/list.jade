extends ../layout

// user
block content
    h1= title
    p Welcome to #{title} create new user
        b: a(href="user/add") +
    ul#users
        each user, i in users
            li: a(href="user/#{user.id}/show") #{user.fname} #{user.lname}
    br
    p: a(href="/") home
